@model IEnumerable<User>

@{
    ViewBag.Title = "Profiles";
}

<div>
    @foreach (var user in Model) {
        <span>@user.Username</span> @Html.Partial("_FollowButtonPartial", user)<br />
    }
</div>

